# 佳佳拍内部针对所有游戏不同系统的通用脚本

1、 需要实现读取订单编号、旺旺号、接单价
2、 不同游戏需要适配不同的录单模板,这个模板最好放在配置文件中，支持修改
3、 通过不同的快捷键调用不同的录单模板
4、 如果存在多笔待发货的订单则需要通过下单时间来做筛选，比如相邻10分钟内的订单合作到一起
5、 自动脚本录单的步骤
  - 通过调用不同游戏录单的快捷键来获取这次使用那个模板，然后读取千牛客服端的当前老板的信息，订单号，旺旺号，接单价等，
  - 将获取到的信息填充到我订单的模板中，其余的项都使用模板定义的默认值即可
  - 录单模板组装好了过后将其粘贴到输入区，
  - 等待客户填完剩余的补充资料后，使用快捷键将其录入到本系统中。
  - 如果录单过程中遇到错误则需要调用window的提示来提示当前客服自动录单那个步骤出问题了，人工好介入完成录单操作
  - 只需要提示错误，不要影响后续录单快捷键使用